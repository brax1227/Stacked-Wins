#
# API base URL.
# - Recommended for local dev: use Vite proxy (no CORS headaches)
# - For direct calls (no proxy): use http://localhost:3001 or http://localhost:3001/api
#
VITE_API_URL=/api
VITE_APP_NAME=Stacked Wins
